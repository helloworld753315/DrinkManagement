version: "3"
services:
  app:
    container_name: app
    build: 
      context: ./docker
      dockerfile: Dockerfile
    ports:
        - 8080:8080
    volumes: 
        - ./drink_management:/app:cached
